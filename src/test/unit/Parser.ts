import { parser } from '../../core/Parser';
import { patterns } from '../data';
import types from '../../types';

export default () => describe('Parser', () => {
  it('asdsad', () => {
    const _pat =  parser(types, patterns, {});
  });
});
